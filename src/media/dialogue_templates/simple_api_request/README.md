# 🌐 Шаблон диалога «Простой API-запрос»

Шаблон демонстрирует простой запрос к открытому API и использование плейсхолдера `<response[...]>` для вывода полей ответа.

---

## 📌 Сценарий

1. **Приветствие** — знакомство с функционалом
2. **Вопрос** — пользователь вводит имя
3. **API-запрос** — GET на `https://api.agify.io/?name=<answers[1]>`
4. **Вывод результата** — возраст и количество наблюдений из ответа API
5. **Завершение** — подтверждение успешной работы

<img src="dialogue_templates/simple_api_request/demo.gif" alt="Демонстрация" width="400">

---

## 🔧 Плейсхолдеры и подстановки

### Доступ к ответам пользователя
- `<answers[1]>` — текст ответа из первого вопроса
- `<answers[2]>` — текст ответа из второго вопроса
- и т.д.

### Доступ к данным из API-ответа
- `<response["age"]>` — значение поля `age` из JSON-ответа
- `<response["count"]>` — значение поля `count` из JSON-ответа
- Поддерживается вложенность: `<response["data"]["id"]>`

---

## 📤 Пример ответа API

API `agify.io` возвращает JSON следующего формата:

```json
{
  "name": "Ivan",
  "age": 42,
  "count": 12345
}
```

Эти поля можно использовать в текстовых блоках:

```
Имя: <answers[1]>
Предполагаемый возраст: <response["age"]>
Наблюдений: <response["count"]>
```

---

## ⚙️ Команды и кнопки

| Тип пользователя | Команда / Кнопка | Действие |
| ---------------- | ----------------- | -------- |
| Любой пользователь | `🌐 API-запрос` | Начать демонстрацию работы с API |

---

## 🌐 Используемое API

- **Сервис**: [agify.io](https://agify.io/)
- **Описание**: Предсказывает возраст по имени на основе статистических данных
- **Метод**: GET
- **Endpoint**: `https://api.agify.io/?name={name}`
- **Авторизация**: не требуется (открытый API)

---

## ✅ Рекомендации

* Вы можете заменить API на любой другой открытый или свой сервис
* В `url`, `headers`, `body` API-блока можно использовать `<answers[число]>` для подстановки данных пользователя
* В последующих текстовых блоках используйте `<response[...]>` для доступа к JSON-ответу
* Для вложенных полей используйте синтаксис: `<response["data"]["items"][0]["name"]>`
* Убедитесь, что API доступен и возвращает корректный JSON

